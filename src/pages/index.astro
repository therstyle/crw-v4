---
import Layout from '../layouts/Layout.astro'
import MainNav from '../components/MainNav.svelte'
import SectionIntro from '../components/SectionIntro.svelte'
import SectionResume from '../components/SectionResume.svelte'
import SectionPortfolio from '../components/SectionPortfolio.svelte'
import SectionContact from '../components/SectionContact.svelte'
import SectionBgVideo from '../components/SectionBgVideo.svelte'
import { nav } from './index.data'
import { intro } from './index.data'
import { portfolio } from './index.data'
import { resume } from './index.data'
import { contact } from './index.data'
import { video } from './index.data'
---

<Layout>
  <main class="crw-main">
    <div class="crw-main__nav">
      <MainNav logo={nav.logo} client:load />
    </div>

    <div class="crw-main__content">
      <SectionIntro {...intro} client:visible />
      <SectionResume {...resume} client:visible />
      <SectionPortfolio {...portfolio} client:visible />
      <SectionContact {...contact} client:visible />
    </div>

    <SectionBgVideo {...video} client:load />
  </main>
</Layout>

<style lang="scss">
  @use '../styles/vars';

  .crw-main {
    display: grid;
    grid-template-columns: calc(var(--main-nav-width) + var(--space-1)) 1fr;
    grid-template-areas: 'nav content';

    &__nav {
      grid-area: nav;
      z-index: 10;
    }

    &__content {
      grid-area: content;
      display: flex;
      flex-direction: column;
      gap: var(--space-8);
    }
  }
</style>
